{
  "name": "auth0-test-sample",
  "version": "1.0.0",
  "description": "An example repository showcasing how to create automated tests for Auth0 custom database functions",
  "main": "index.js",
  "author": "Thiago Martins",
  "license": "MIT",
  "private": false,
  "engines": {
    "node": ">=20.11.1"
  },
  "scripts": {
    "pull": "a0deploy export -c config.json --format=yaml --output_folder=tenant",
    "test": "node --test"
  },
  "devDependencies": {
    "auth0-deploy-cli": "^7.21.0"
  }
}
